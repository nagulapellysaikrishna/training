package com.service;

public class question9service {
	public static int sumOfSquaresOfEvenDigits(int n) {
		int a;
		int square = 0;
		while(n>0) {
			a = n%10;
			System.out.println(a);
			n = n/10;
			if (a%2==0) {
				square+=Math.pow(a,2);
			}
		}
		
		return square;
		
	}
}
